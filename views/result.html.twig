<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Anagram</title>
    </head>
    <body>
        <h1>Anagram</h1>
        <ul>
          <li>Quarters: {{result["quarters"]}}</li>
          <li>Dimes: {{result["dimes"]}}</li>
          <li>Nickles: {{result["nickles"]}}</li>
          <li>Pennies: {{result["pennies"]}}</li>
          <li>Total Coins: {{result["total"]}}</li>
        </ul>
    </body>
</html>
